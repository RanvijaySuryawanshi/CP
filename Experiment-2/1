#include <iostream>
using namespace std;

int main() {
    int n, pos;
    cout << "Enter number: ";
    cin >> n;
    cout << "Enter bit position: ";
    cin >> pos;

    if (n & (1 << pos))
        cout << "Bit is SET";
    else
        cout << "Bit is NOT SET";

    return 0;
}
