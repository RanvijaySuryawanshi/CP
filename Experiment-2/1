#include <stdio.h>

int main() {
    int N, n;
    printf("Enter number N: ");
    scanf("%d", &N);
    printf("Enter bit position n: ");
    scanf("%d", &n);

    if (N & (1 << n))
        printf("Bit %d is SET\n", n);
    else
        printf("Bit %d is NOT SET\n", n);

    return 0;
}
